<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Gather</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>LSApplicationCategoryType</key>
	<string>public.app-category.productivity</string>
	<key>GoogleMapsAPIKey</key>
	<string>$(GOOGLE_MAPS_API_KEY)</string>

	<!--
	  Export-compliance shortcut. The app uses only Apple-provided
	  encryption (HTTPS, Keychain, standard SDK auth flows) which is
	  exempt under US export rules (EAR 740.17(b)(1)). Setting this to
	  NO skips the TestFlight / App Store Connect prompt on every
	  build upload.
	-->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UILaunchScreen</key>
	<dict/>

	<!--
	  Declare only capabilities required for the whole app to launch.
	  NFC is an optional scan path guarded by NFCReaderSession.readingAvailable;
	  listing "nfc" here makes App Store Connect warn for Mac and Vision Pro,
	  where NFC hardware is unavailable.
	-->
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>

	<!-- Required so iOS lets us open the camera for QR scanning (ScanView). -->
	<key>NSCameraUsageDescription</key>
	<string>GatherHub uses the camera to scan QR codes on club equipment so you can check kit in and out.</string>

	<!-- Required so iOS lets us start a Core NFC reader session (ScanView). -->
	<key>NFCReaderUsageDescription</key>
	<string>GatherHub reads NFC tags on club equipment so you can check kit in and out by tapping the item.</string>

	<!--
	  AID whitelist for ISO 7816 / NFC Type 4 tags. iOS will NOT present
	  the system NFC sheet for an NFCTagReaderSession unless at least one
	  AID is listed here OR the tags are pure ISO 14443 (MIFARE). Most
	  modern NDEF-capable tags negotiate the standard NDEF Tag
	  Application AID D2760000850101 — listing it covers the same range
	  Kit-Trace's flutter_nfc_kit lists. Without this key, .iso14443
	  polling silently fails on some tag types.
	-->
	<key>com.apple.developer.nfc.readersession.iso7816.select-identifiers</key>
	<array>
		<string>D2760000850101</string>
	</array>

	<!-- Required so the scan flow can attach the device's location to a
	     "sighting" audit log when staff scan kit in the field. -->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>GatherHub attaches the device location to asset scans so committee can trace where kit was last seen.</string>

	<!-- Deep links: gatherhub://asset/tag_xxx are handled in GatherHubApp. -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>au.com.gatherhub</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>gatherhub</string>
			</array>
		</dict>
	</array>

	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
